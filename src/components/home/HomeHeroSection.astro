---
import type { Dictionary, Locale } from '../../i18n';
import HomeEditorialPanel from './HomeEditorialPanel.astro';
import HomeHeroActions from './HomeHeroActions.astro';

interface Props {
  locale: Locale;
  messages: Dictionary;
}

const { locale, messages } = Astro.props;
---

<section class="home-hero mx-auto max-w-5xl">
  <div class="grid grid-cols-1 gap-4 md:grid-cols-3">

    <!-- Large hero card (2 cols, spans both right-side rows) -->
    <div
      class="bento-card md:col-span-2 md:row-span-2 flex flex-col justify-end rounded-2xl border border-[hsl(var(--primary-300)/0.25)] bg-[hsl(var(--surface-container-low))] p-8 shadow-[0_12px_36px_-20px_hsl(var(--primary-900)/0.3)] min-h-[340px] home-reveal"
      style="--reveal-delay: 40ms;"
    >
      <div class="space-y-5">
        <span class="inline-block rounded-full border border-[hsl(var(--primary-400)/0.5)] bg-[hsl(var(--primary-100)/0.65)] px-3 py-1 text-[11px] font-semibold uppercase tracking-[0.15em] text-[hsl(var(--primary-800))]">
          {messages.home.badge}
        </span>
        <h1 class="text-4xl font-bold tracking-tight leading-[1.1] text-foreground sm:text-5xl">
          {messages.home.headline}
        </h1>
        <p class="text-base leading-relaxed text-[hsl(var(--muted-foreground))]">
          {messages.home.description}
        </p>
        <HomeHeroActions locale={locale} messages={messages} />
      </div>
    </div>

    <!-- Editorial panel (top-right) -->
    <div class="h-full home-reveal" style="--reveal-delay: 130ms;">
      <HomeEditorialPanel messages={messages} />
    </div>

    <!-- Focus / latest teaser card (bottom-right) -->
    <div
      class="bento-card flex flex-col justify-between rounded-2xl border border-[hsl(var(--primary-300)/0.12)] bg-[hsl(var(--surface-container))] p-6 home-reveal"
      style="--reveal-delay: 210ms;"
    >
      <span class="text-[10px] font-semibold uppercase tracking-[0.2em] text-[hsl(var(--muted-foreground))]">
        {messages.blog.latestTitle}
      </span>
      <p class="mt-3 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">
        {messages.blog.latestDescription}
      </p>
    </div>

  </div>
</section>
